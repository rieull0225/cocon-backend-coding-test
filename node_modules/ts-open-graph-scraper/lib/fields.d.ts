import { OGFields } from './types';
declare type FieldMapping = {
    multiple: boolean;
    property: string;
    fieldName: OGFields;
};
declare const fields: FieldMapping[];
export default fields;
